# Codex 진입점

작업 전에 같은 디렉터리의 `CODEX.md`를 읽고, 그 파일이 지정하는 `CLAUDE.md`와 필요한 워크플로우 문서를 실제로 열어 적용한다.
링크나 `@경로` 표기만으로 외부 파일 내용이 자동 주입된다고 가정하지 않는다.
공통 규칙의 원본은 `CLAUDE.md`이며, Codex 실행 환경의 차이는 `CODEX.md`에서 설명한다.

## 요약

- `CODEX.md`를 통해 기존 Harness 공통 규칙을 읽는다.

## 사용자 결정 사항

1. 기존 Claude 작업 절차를 Codex에서도 사용할 수 있게 연결한다.

## 사용자 확인 사항

없음
